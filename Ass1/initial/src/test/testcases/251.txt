class abc {
        				string static getArea(length,width:float){
							for x := 5 + a.getT() downto 2 do
								io.writeIntLn(x);
							for i := 1 - b.getN() to 100 do {
								io.writeIntLn(i);
								Intarray[i] := i + 1;
							}

							return this.a;
						}
					}