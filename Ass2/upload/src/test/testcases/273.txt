class abc {
        				void static getArea(length,width:float){
							if a == 1 then
								io.write("Suy nua thi");
								if b < 0 then
									b := c + 3;
								else
									b[1] := a;
									for i := 1 to 100 do {
										io.writeIntLn(i);
										Intarray[i] := i + 1;
									}
						}
					}