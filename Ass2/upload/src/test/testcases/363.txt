class abc {
                        void static getArea(length,width:float){
                            {
                                this.length := length;
                                this.length := ! length;
                                a := a + [a + 1];
                                return a;
                                
                            }
                        }
                    }