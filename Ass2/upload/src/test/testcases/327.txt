class abc {
                        int static getArea(length,width:float){
                            for x := 5 downto 2 do
                                io.writeIntLn(x);
                            for i := 1 to 100 do {
                                if a < 1 then
                                    continue;
                                else
                                    break;
                                io.writeIntLn(i);
                                Intarray[i] := i + 1;
                            }

                            return 0;
                        }
                    }