class abc {
                        void getArea(a, b, c, d:string){
                            {
                                this.length := length;
                                this.length := ! length;
                                a := a + [a + b % 10];
                                if(a == b) then
                                    io.write("ABC");
                                    for a := 1*b.get("length") to 100 do
                                        tong := tong + 1;
                                return tong;
                                
                            }
                        }
                    }